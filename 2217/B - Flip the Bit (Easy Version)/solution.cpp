#include <bits/stdc++.h>using namespace std;  int main(){    ios::sync_with_stdio(false);    cin.tie(0);    int t;    cin>>t;    while(t--){    int n,k;    cin>>n>>k;    vector<int> arr(n+2);    for(int i=1;i<=n;i++){        cin>>arr[i];    }    int pivot;    cin>>pivot;     arr[0]= arr[n+1] = arr[pivot];    int countl =0 ;int countr=0;    for(int i=0;i<pivot;i++){        if(arr[i]!=arr[i+1]){            countl++;        }    }    for(int i=pivot;i< n+1;i++){        if(arr[i] != arr[i+1]){            countr++;        }    }    cout<<max(countl,countr)<<endl; }}